# Environment Variables Configuration

# ==================================================
# สำหรับการ Deploy บน Netlify
# ==================================================
# ตั้งค่าใน: Netlify Dashboard → Site Settings → Environment Variables
# หรือใช้ Netlify CLI: netlify env:set VARIABLE_NAME "value"

# Node.js Version
# ระบุเวอร์ชัน Node.js ที่ต้องการใช้ในการ build
NODE_VERSION=18

# ==================================================
# Frontend Configuration (React App)
# ==================================================
# URL ของ API สำหรับ production
# แทนที่ YOUR-SITE-NAME ด้วยชื่อ Netlify site ของคุณ
REACT_APP_API_URL=https://YOUR-SITE-NAME.netlify.app/.netlify/functions

# Base URL ของ Application (ถ้า deploy ที่ subdirectory)
# ถ้า deploy ที่ root (/) ไม่ต้องตั้งค่า
# PUBLIC_URL=/

# ==================================================
# Build Configuration
# ==================================================
# ปิดการ treat warnings เป็น errors ในการ build
# ถ้าอยากให้ warnings เป็น errors ให้ตั้งเป็น true
CI=false

# ==================================================
# Netlify Specific (Optional)
# ==================================================
# กำหนด Build Command (override netlify.toml)
# NETLIFY_BUILD_COMMAND=npm run build

# กำหนด Publish Directory (override netlify.toml)
# NETLIFY_PUBLISH_DIR=build

# ==================================================
# Functions Configuration (Optional)
# ==================================================
# Timeout สำหรับ Serverless Functions (milliseconds)
# Default: 10000 (10 seconds)
# Maximum: 26000 (26 seconds) สำหรับ Free tier
# FUNCTIONS_TIMEOUT=15000

# ==================================================
# Debug & Logging (Development Only)
# ==================================================
# เปิด debug logs สำหรับการ develop
# DEBUG=netlify:*

# ==================================================
# Third-party Services (ถ้ามี)
# ==================================================
# ถ้าในอนาคตต้องการเพิ่ม services เช่น:
# - Analytics
# - Error tracking (Sentry)
# - Database URLs
# - API Keys

# ตัวอย่าง:
# REACT_APP_SENTRY_DSN=https://xxx@sentry.io/xxx
# REACT_APP_ANALYTICS_ID=UA-XXXXX-X

# ==================================================
# สำหรับการพัฒนา (Local Development)
# ==================================================
# สร้างไฟล์ .env.local ในเครื่องของคุณ (ไม่ commit ขึ้น Git)
# และเพิ่ม:
#
# REACT_APP_API_URL=http://localhost:8888/.netlify/functions
#
# จากนั้นรัน:
# netlify dev
#
# Application จะใช้ค่าจาก .env.local แทนค่า production

# ==================================================
# Important Notes
# ==================================================
# 1. ตัวแปรที่ขึ้นต้นด้วย REACT_APP_ จะสามารถเข้าถึงได้ใน frontend code:
#    const apiUrl = process.env.REACT_APP_API_URL;
#
# 2. ตัวแปรที่ไม่มี REACT_APP_ จะใช้ได้ใน build process และ functions เท่านั้น
#
# 3. ค่าเหล่านี้จะถูก embed ลงใน build artifacts
#    ดังนั้น ห้ามใส่ secret keys หรือ sensitive data ใน REACT_APP_*
#
# 4. สำหรับ secrets (API keys, database passwords) ควรใช้:
#    - Netlify Environment Variables (ไม่ขึ้นต้นด้วย REACT_APP_)
#    - เข้าถึงจาก Netlify Functions ด้วย process.env.SECRET_NAME
#
# 5. การเปลี่ยนค่า environment variables บน Netlify
#    จะต้อง trigger rebuild ใหม่เพื่อให้มีผล

# ==================================================
# Sensitive Data - DO NOT COMMIT
# ==================================================
# ถ้ามี sensitive data ให้สร้างไฟล์ .env.local
# และเพิ่ม .env.local ลงใน .gitignore
